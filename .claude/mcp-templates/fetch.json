{
  "name": "fetch",
  "description": "Web content fetching — retrieve documentation, API responses, and web pages",
  "config": {
    "mcpServers": {
      "fetch": {
        "command": "npx",
        "args": ["-y", "@anthropic-ai/mcp-server-fetch"]
      }
    }
  }
}
